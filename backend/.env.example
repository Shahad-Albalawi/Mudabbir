APP_NAME=Mudabbir
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# SQLite: create empty database/database.sqlite before migrate.
# DB_DATABASE=/absolute/path/to/custom.sqlite
#
# Laravel Telescope (local development only — disabled in production/CI)
TELESCOPE_ENABLED=true
#
# Neon PostgreSQL — use pooled DATABASE_URL (-pooler host). See docs/neon-env.example.
# DB_CONNECTION=pgsql
# DATABASE_URL=postgresql://USER:PASS@ep-xxxx-pooler.region.aws.neon.tech/neondb?sslmode=require
# HEALTH_DB_TIMEOUT_SECONDS=5
#
# MySQL example:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=mudabbir
# DB_USERNAME=root
# DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# AI coach: openai | gemini
AI_PROVIDER=openai

OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
OPENAI_TIMEOUT_SECONDS=30
OPENAI_CONNECT_TIMEOUT_SECONDS=10
OPENAI_RETRIES=2
OPENAI_RETRY_SLEEP_MS=250
OPENAI_VERIFY_SSL=true

GEMINI_API_KEY=
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta
GEMINI_MODEL=gemini-2.0-flash
GEMINI_TIMEOUT_SECONDS=30
GEMINI_CONNECT_TIMEOUT_SECONDS=10
GEMINI_RETRIES=2
GEMINI_RETRY_SLEEP_MS=250
GEMINI_VERIFY_SSL=true

# Firebase Cloud Messaging (budget alerts)
FCM_SERVER_KEY=

# Sanctum personal access token lifetime in minutes (default 30 days).
SANCTUM_TOKEN_EXPIRATION=43200

# Sentry error monitoring (production). Free tier: ~5000 events/month.
# Create project at https://sentry.io — leave empty locally to disable.
SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.0

# Reverse proxy trust (Render/production: *). Local: leave empty.
TRUSTED_PROXIES=

# Browser / Flutter Web CORS (comma-separated). Defaults in config/cors.php:
# https://yourdomain.com,http://localhost:3000,http://localhost:8080
CORS_ALLOWED_ORIGINS=
